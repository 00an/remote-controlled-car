// Copy this file to config_secrets.h and fill in your Wi-Fi credentials.
// config_secrets.h is git-ignored so credentials never enter version control.
#pragma once

const char* ssid = "";
const char* password = "";
